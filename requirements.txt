# Deployable core. NO TensorFlow.
#
# Streamlit Community Cloud auto-discovers this file -- it cannot be pointed at
# a different filename -- so requirements.txt must be the one that actually
# deploys. TensorFlow is the problem child: it publishes no wheels for the
# Python 3.14 that Streamlit Cloud now provisions, and at ~1.5GB it would blow
# the ~1GB resource ceiling regardless.
#
# Nothing in the serving path needs it. TensorFlow is imported lazily and only
# when a promoted bundle actually contains a keras model, and the promotion
# guard in the training pipeline prefers deterministic models precisely so the
# serving environment stays light.
#
# Training uses requirements-train.txt, which layers TensorFlow on top of this.

# ---- core ----
numpy>=1.26,<3
pandas>=2.2
pyarrow>=16.0
requests>=2.32
python-dotenv>=1.0
joblib>=1.4
tzdata>=2024.1            # zoneinfo has no bundled tz database on Windows

# ---- ml ----
scikit-learn>=1.5
xgboost>=2.1

# ---- explainability ----
shap>=0.46
lime>=0.2.0.1

# ---- app / api ----
streamlit>=1.38
plotly>=5.24
flask>=3.0
flask-cors>=5.0
gunicorn>=23.0 ; sys_platform != "win32"
waitress>=3.0 ; sys_platform == "win32"

# ---- monitoring ----
prometheus-client>=0.21

# ---- eda ----
matplotlib>=3.9
seaborn>=0.13
